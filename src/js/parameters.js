/**
 * Class with default value parameters
 */
class Parameters {
    construct() {
        // empty, for now
    }


    /**
     * Returts default values
     * @returns {{ship: {generic: {speed: number}, player: {animIdle: string}}}}
     */
    getValues() {
        return {
            // empty, for now
        };
    }
}
